# Claude Code 서브에이전트 & 스킬 학습 프로젝트

Claude Code의 **서브에이전트(Subagents)**와 **스킬(Skills)** 기능을 실습하고 깊이 이해하기 위한 MVP 프로젝트입니다.

## 📚 목차

- [프로젝트 개요](#프로젝트-개요)
- [핵심 개념 이해](#핵심-개념-이해)
  - [서브에이전트란?](#서브에이전트란)
  - [스킬이란?](#스킬이란)
  - [서브에이전트 vs 스킬](#서브에이전트-vs-스킬)
- [설치 및 설정](#설치-및-설정)
- [프로젝트 구조](#프로젝트-구조)
- [서브에이전트 상세 가이드](#서브에이전트-상세-가이드)
- [스킬 상세 가이드](#스킬-상세-가이드)
- [실습 시나리오](#실습-시나리오)
- [서브에이전트 체이닝](#서브에이전트-체이닝)
- [개발 가이드](#개발-가이드)
- [문제 해결](#문제-해결)
- [참고 자료](#참고-자료)

---

## 프로젝트 개요

- **목적**: Claude Code의 확장 기능 실습 및 이해
- **언어**: 한국어 (기본), Python & TypeScript/JavaScript (개발)
- **주요 기능**: 커스텀 서브에이전트와 스킬 예제 제공
- **실습 내용**:
  - 보안 코드 리뷰 자동화
  - 테스트 주도 개발
  - Git 커밋 메시지 자동 생성
  - 데이터 분석 파이프라인

---

## 핵심 개념 이해

### 서브에이전트란?

**서브에이전트(Subagent)**는 특정 전문 분야를 담당하는 독립적인 AI 에이전트입니다.

#### 🎯 주요 특징

1. **전문성**: 각 서브에이전트는 특정 도메인(코드 리뷰, 테스트, 데이터 분석 등)에 특화
2. **독립 실행**: 메인 에이전트로부터 독립적으로 실행되며, 자체 도구와 컨텍스트 보유
3. **재사용성**: 한 번 정의하면 프로젝트 전체에서 반복 사용 가능
4. **조합 가능**: 여러 서브에이전트를 체이닝하여 복잡한 워크플로우 구축

#### 🔧 동작 방식

```
User Request
    ↓
Main Agent (판단)
    ↓
Subagent 호출 (Task tool)
    ↓
Subagent 실행 (독립적)
    ├─ Read files
    ├─ Run commands
    ├─ Analyze code
    └─ Generate report
    ↓
Main Agent (결과 수신)
    ↓
User Response
```

#### 📝 정의 위치

- 파일 위치: `.claude/agents/*.md`
- 형식: Markdown with YAML frontmatter

**예시 구조:**
```markdown
---
name: code-reviewer
description: 코드 품질과 보안을 검토하는 전문가
tools: Read, Grep, Glob, Bash
model: sonnet
---

당신은 시니어 코드 리뷰어입니다...
```

---

### 스킬이란?

**스킬(Skill)**은 특정 작업을 수행하는 방법을 설명하는 가이드이자 자동 활성화되는 워크플로우입니다.

#### 🎯 주요 특징

1. **자동 활성화**: 특정 키워드 감지 시 자동으로 실행
2. **가이드 제공**: 메인 에이전트에게 작업 수행 방법 안내
3. **경량**: 서브에이전트보다 가볍고 빠름
4. **컨텍스트 공유**: 메인 에이전트와 동일한 컨텍스트 사용

#### 🔧 동작 방식

```
User: "커밋 메시지 작성해줘"
    ↓
Main Agent (키워드 감지: "커밋 메시지")
    ↓
Skill 활성화 (commit-helper)
    ├─ SKILL.md 내용 로드
    ├─ 가이드 따라 실행
    │   ├─ git diff --staged
    │   ├─ 변경사항 분석
    │   └─ 커밋 메시지 생성
    └─ 결과 반환
    ↓
User: 커밋 메시지 수신
```

#### 📝 정의 위치

- 디렉토리 구조:
  ```
  .claude/skills/
  └── your-skill-name/
      └── SKILL.md
  ```

**예시 구조:**
```markdown
---
name: commit-helper
description: Git 변경사항을 분석하여 커밋 메시지 생성. 키워드: 커밋, commit
allowed-tools: Bash, Read, Grep
---

# Commit Helper Skill

이 스킬은 Conventional Commits 형식으로...
```

---

### 서브에이전트 vs 스킬

| 구분 | 서브에이전트 (Subagent) | 스킬 (Skill) |
|------|------------------------|-------------|
| **목적** | 복잡한 전문 작업 수행 | 간단한 워크플로우 가이드 |
| **실행 방식** | 독립 에이전트로 분리 실행 | 메인 에이전트가 직접 실행 |
| **컨텍스트** | 독립적인 컨텍스트 | 메인 에이전트와 공유 |
| **활성화** | 명시적 호출 또는 자동 판단 | 키워드 자동 감지 |
| **복잡도** | 높음 (상태, 도구, 프롬프트) | 낮음 (가이드 문서) |
| **속도** | 느림 (독립 실행) | 빠름 (즉시 실행) |
| **사용 예시** | 코드 리뷰, 테스트 분석 | 커밋 메시지, 포맷팅 |

#### 🤔 언제 무엇을 사용할까?

**서브에이전트를 사용하세요:**
- ✅ 복잡한 분석이 필요할 때 (코드 보안 검토)
- ✅ 여러 도구를 조합해야 할 때
- ✅ 독립적인 의사결정이 필요할 때
- ✅ 재사용 가능한 전문가가 필요할 때

**스킬을 사용하세요:**
- ✅ 간단한 반복 작업일 때 (커밋 메시지 작성)
- ✅ 빠른 응답이 필요할 때
- ✅ 메인 컨텍스트를 활용할 때
- ✅ 가이드만 제공하면 충분할 때

---

## 설치 및 설정

이 프로젝트는 **Python**과 **TypeScript/JavaScript** 두 가지 언어로 예제를 제공합니다.

### Python 환경 설정

#### 1. 의존성 설치

```bash
pip install -r requirements.txt
```

#### 2. 환경 확인

```bash
# Python 버전 확인 (3.8+)
python --version

# pytest 설치 확인
pytest --version

# 설치 완료 테스트
pytest
```

### TypeScript/JavaScript 환경 설정

#### 1. Node.js 설치 확인

```bash
# Node.js 버전 확인 (v18+)
node --version

# npm 버전 확인
npm --version
```

#### 2. 의존성 설치

```bash
npm install
```

#### 3. TypeScript 예제 실행

```bash
# 웹 스크레이퍼 실행
npm run scraper

# API 클라이언트 실행
npm run api

# 데이터 프로세서 실행
npm run processor

# 또는 직접 실행
tsx examples/web-scraper.ts
tsx examples/api-client.ts
tsx examples/data-processor.ts
```

---

## 프로젝트 구조

```
sub_agent_skills/
├── .claude/                    # Claude Code 설정
│   ├── agents/                 # 서브에이전트 정의
│   │   ├── code-reviewer.md    # 코드 리뷰 전문가
│   │   ├── test-runner.md      # 테스트 실행 전문가
│   │   └── data-analyzer.md    # 데이터 분석 전문가
│   └── skills/                 # 스킬 정의
│       ├── commit-helper/      # 커밋 메시지 생성
│       │   └── SKILL.md
│       └── test-generator/     # 테스트 자동 생성
│           └── SKILL.md
├── examples/                   # 예제 코드
│   ├── python/                 # Python 예제
│   │   ├── web_scraper.py      # BeautifulSoup 웹 스크레이퍼
│   │   ├── data_processor.py   # pandas 데이터 처리
│   │   └── api_client.py       # requests API 클라이언트
│   └── typescript/             # TypeScript 예제
│       ├── web-scraper.ts      # Cheerio 웹 스크레이퍼
│       ├── data-processor.ts   # 네이티브 데이터 처리
│       └── api-client.ts       # axios API 클라이언트
├── tests/                      # 테스트 코드 (Python)
│   └── test_web_scraper.py     # 47개 포괄적 테스트
├── CLAUDE.md                   # Claude Code 가이드
├── requirements.txt            # Python 의존성
├── package.json                # Node.js 의존성
├── tsconfig.json               # TypeScript 설정
└── README.md                   # 이 파일
```

---

## 서브에이전트 상세 가이드

### 1. code-reviewer (코드 리뷰어)

**역할**: 코드 품질, 보안, 베스트 프랙티스를 전문적으로 검토

#### 📋 검토 항목

1. **보안 취약점 (Critical)**
   - SQL Injection
   - XSS (Cross-Site Scripting)
   - SSRF (Server-Side Request Forgery)
   - Path Traversal
   - Command Injection
   - 민감 정보 노출

2. **코드 품질**
   - 가독성 및 명확성
   - 함수 단일 책임 원칙
   - 중복 코드 제거
   - 적절한 추상화

3. **에러 처리**
   - 예외 처리 적절성
   - 에러 메시지 유용성
   - 실패 시나리오 고려

4. **성능**
   - 불필요한 루프 최적화
   - 데이터베이스 쿼리 효율성
   - 메모리 사용 최적화

#### 🔧 사용 도구

- **Read**: 파일 내용 읽기
- **Grep**: 코드 패턴 검색
- **Glob**: 파일 찾기
- **Bash**: Git diff, 테스트 실행

#### 💬 사용 방법

```bash
# 명시적 호출
> Use the code-reviewer subagent to review my recent changes

# 파일 지정
> @examples/web_scraper.py 이 코드 리뷰해줘

# 자동 활성화 (코드 작성 직후)
> 방금 작성한 코드 검토 필요
```

#### 📊 출력 형식

```markdown
## 코드 리뷰 결과

### 🔴 Critical Issues (즉시 수정 필요)
- [web_scraper.py:51] SQL Injection 취약점
  설명: 사용자 입력이 직접 쿼리에...
  해결: Prepared Statement 사용

### 🟡 Warnings (수정 권장)
- [api_client.py:120] 에러 처리 누락
  설명: 네트워크 오류 시...

### 🟢 Suggestions (개선 제안)
- [data_processor.py:45] 리스트 컴프리헨션 사용 권장
```

#### 🎓 실전 예시

**입력:**
```python
def fetch_user_data(user_id):
    # 보안 취약점: SQL Injection
    query = f"SELECT * FROM users WHERE id = {user_id}"
    return db.execute(query)
```

**code-reviewer 분석:**
```
🔴 Critical: SQL Injection 취약점 발견
- 위치: user_service.py:42
- 설명: 사용자 입력이 직접 SQL 쿼리에 삽입됨
- 위험: 악의적 사용자가 데이터베이스 전체 접근 가능
- 해결:
  ```python
  def fetch_user_data(user_id):
      query = "SELECT * FROM users WHERE id = ?"
      return db.execute(query, (user_id,))
  ```
```

---

### 2. test-runner (테스트 실행자)

**역할**: pytest 자동 실행 및 결과 분석

#### 📋 주요 기능

1. **테스트 실행**
   - 전체 테스트 스위트 실행
   - 특정 파일/함수만 실행
   - 커버리지 리포트 생성

2. **결과 분석**
   - 실패 원인 파악
   - 스택 트레이스 해석
   - 수정 제안

3. **커버리지 검토**
   - 미테스트 코드 식별
   - 커버리지 목표 달성 확인

#### 🔧 사용 도구

- **Bash**: pytest 실행
- **Read**: 테스트 파일 읽기
- **Grep**: 테스트 패턴 검색

#### 💬 사용 방법

```bash
# 전체 테스트 실행
> Use the test-runner subagent to run all tests

# 커버리지 포함
> Run tests with coverage report

# 자동 활성화
> 테스트 돌려줘
```

#### 📊 출력 형식

```markdown
## 테스트 실행 결과

### 요약
- 총 테스트: 47개
- 통과: 47개 (100%)
- 실패: 0개
- 커버리지: 92%

### 커버리지 분석
- web_scraper.py: 74% (39줄 미테스트)
  - 미테스트 라인: 297-363 (main 함수)
  - 권장: main 함수는 테스트 불필요

### 권장사항
1. DNS Rebinding 테스트 추가 ✅ 완료
2. Edge case 테스트 확대
```

---

### 3. data-analyzer (데이터 분석가)

**역할**: 데이터 분석, 시각화, 통계 계산

#### 📋 주요 기능

1. **데이터 로드**
   - CSV, JSON, Excel 지원
   - 데이터 유효성 검증

2. **통계 분석**
   - 기초 통계 (평균, 중앙값, 표준편차)
   - 상관관계 분석
   - 이상치 탐지

3. **시각화**
   - matplotlib/seaborn 차트 생성
   - 트렌드 분석

#### 🔧 사용 도구

- **Bash**: Python 스크립트 실행
- **Read**: 데이터 파일 읽기
- **Write**: 리포트 생성

#### 💬 사용 방법

```bash
# CSV 파일 분석
> Use the data-analyzer subagent to analyze data.csv

# 시각화 요청
> 이 데이터의 트렌드를 차트로 보여줘
```

---

## 스킬 상세 가이드

### 1. commit-helper (커밋 헬퍼)

**역할**: Conventional Commits 형식의 커밋 메시지 자동 생성

#### 📋 커밋 메시지 형식

```
<type>(<scope>): <subject>

<body>

<footer>
```

#### 🏷️ Type 종류

| Type | 설명 | 예시 |
|------|------|------|
| `feat` | 새 기능 추가 | `feat(auth): 소셜 로그인 추가` |
| `fix` | 버그 수정 | `fix(api): 타임아웃 오류 수정` |
| `docs` | 문서 변경 | `docs(readme): 설치 가이드 추가` |
| `style` | 코드 포맷팅 | `style: 들여쓰기 정리` |
| `refactor` | 리팩토링 | `refactor(db): 쿼리 최적화` |
| `test` | 테스트 추가/수정 | `test(user): 회원가입 테스트 추가` |
| `chore` | 빌드/설정 변경 | `chore: 의존성 업데이트` |
| `perf` | 성능 개선 | `perf(cache): Redis 캐싱 추가` |
| `security` | 보안 강화 | `security: XSS 방어 추가` |

#### 💬 자동 활성화 키워드

- "커밋 메시지"
- "commit message"
- "스테이징된 변경사항"
- "git commit"

#### 🎓 실전 예시

**시나리오**: DNS Rebinding 보호 기능 추가

**입력:**
```bash
> 커밋 메시지 작성해줘
```

**commit-helper 실행:**
1. `git diff --staged` 실행
2. 변경사항 분석:
   - DNS 검증 로직 39줄 추가
   - lxml 파서 변경 3곳
   - 테스트 140+ 줄 추가
3. Type 결정: `security` (보안 강화)
4. Scope 결정: `web-scraper`

**생성된 메시지:**
```
security(web-scraper): DNS 리바인딩 공격 방어 및 보안 강화

주요 변경사항:

1. DNS 리바인딩 공격 방어 구현
   - _validate_url() 메서드에 DNS 해석 검증 로직 추가
   - socket.getaddrinfo()를 사용한 DNS 해석
   - 루프백, 사설 IP, 링크-로컬 주소 차단
   - AWS 메타데이터 엔드포인트 차단

2. BeautifulSoup 파서 보안 개선
   - 'html.parser'에서 'lxml'로 업그레이드
   - 보안성과 성능 향상

3. 포괄적인 테스트 스위트 추가
   - 초기화 테스트 11개
   - DNS 리바인딩 방어 테스트 6개
   - 전체 47개 테스트 100% 통과

이번 보안 업데이트는 SSRF 공격 벡터를 차단하고,
내부 네트워크 및 클라우드 메타데이터 접근을 방지합니다.
```

#### ✅ 커밋 메시지 체크리스트

- [ ] Type이 적절한가?
- [ ] Subject가 50자 이하인가?
- [ ] 명령문 형식인가? ("추가한다" ❌ → "추가" ✅)
- [ ] 변경 이유가 설명되었는가?
- [ ] Breaking change 표시했는가?
- [ ] 관련 이슈 참조했는가?

---

### 2. test-generator (테스트 생성기)

**역할**: Python 함수/클래스에 대한 pytest 테스트 자동 생성

#### 📋 생성하는 테스트 종류

1. **정상 케이스 (Happy Path)**
   - 기대되는 입력과 출력
   - 일반적인 사용 시나리오

2. **엣지 케이스 (Edge Cases)**
   - 경계값 테스트
   - 빈 입력, null 값
   - 매우 큰 값, 매우 작은 값

3. **에러 케이스 (Error Cases)**
   - 예외 발생 시나리오
   - 잘못된 입력 처리

4. **Mock 객체**
   - 외부 의존성 모킹
   - 네트워크, 파일 시스템, DB

#### 🎯 AAA 패턴 (Arrange-Act-Assert)

```python
def test_example():
    # Arrange (준비): 테스트 데이터 설정
    scraper = WebScraper("https://example.com")

    # Act (실행): 테스트 대상 함수 실행
    result = scraper.fetch_page("https://example.com")

    # Assert (검증): 결과 확인
    assert result is not None
    assert "example" in result
```

#### 💬 자동 활성화 키워드

- "테스트 생성"
- "generate tests"
- "unit test"
- "테스트 작성"

#### 🎓 실전 예시

**입력 코드:**
```python
def calculate_total(items, tax_rate):
    """상품 총액 계산"""
    subtotal = sum(item['price'] for item in items)
    tax = subtotal * tax_rate
    return subtotal + tax
```

**입력 요청:**
```bash
> calculate_total 함수에 대한 테스트 생성해줘
```

**test-generator 생성 결과:**
```python
import pytest

class TestCalculateTotal:
    """calculate_total 함수 테스트"""

    def test_calculate_total_success(self):
        """정상 케이스: 일반적인 상품 목록"""
        # Arrange
        items = [
            {'name': '상품A', 'price': 1000},
            {'name': '상품B', 'price': 2000}
        ]
        tax_rate = 0.1

        # Act
        result = calculate_total(items, tax_rate)

        # Assert
        assert result == 3300  # (1000 + 2000) * 1.1

    def test_calculate_total_empty_items(self):
        """엣지 케이스: 빈 상품 목록"""
        # Arrange
        items = []
        tax_rate = 0.1

        # Act
        result = calculate_total(items, tax_rate)

        # Assert
        assert result == 0

    def test_calculate_total_zero_tax(self):
        """엣지 케이스: 세금 0%"""
        # Arrange
        items = [{'name': '상품A', 'price': 1000}]
        tax_rate = 0

        # Act
        result = calculate_total(items, tax_rate)

        # Assert
        assert result == 1000

    def test_calculate_total_high_tax(self):
        """엣지 케이스: 높은 세율"""
        # Arrange
        items = [{'name': '상품A', 'price': 1000}]
        tax_rate = 1.0  # 100%

        # Act
        result = calculate_total(items, tax_rate)

        # Assert
        assert result == 2000

    def test_calculate_total_invalid_items(self):
        """에러 케이스: price 키 없음"""
        # Arrange
        items = [{'name': '상품A'}]  # price 없음
        tax_rate = 0.1

        # Act & Assert
        with pytest.raises(KeyError):
            calculate_total(items, tax_rate)
```

---

## 실습 시나리오

### 시나리오 1: 코드 리뷰 자동화

**목표**: 보안 취약점이 있는 코드를 작성하고, code-reviewer가 자동으로 발견하도록 함

**단계:**

1. **취약한 코드 작성**
   ```python
   # bad_example.py
   def get_user_data(user_id):
       # SQL Injection 취약점
       query = f"SELECT * FROM users WHERE id = {user_id}"
       return db.execute(query)
   ```

2. **서브에이전트 호출**
   ```bash
   > @bad_example.py 코드 리뷰해줘
   ```

3. **결과 분석**
   - 🔴 Critical: SQL Injection 발견
   - 🟡 Warning: 에러 처리 누락
   - 수정 방법 제시

4. **코드 수정**
   ```python
   def get_user_data(user_id):
       query = "SELECT * FROM users WHERE id = ?"
       return db.execute(query, (user_id,))
   ```

5. **재검토**
   ```bash
   > 다시 리뷰해줘
   ```

**예상 소요 시간**: 5분

---

### 시나리오 2: 테스트 주도 개발 (TDD)

**목표**: 함수 작성 → 테스트 생성 → 테스트 실행의 전체 사이클 경험

**단계:**

1. **함수 작성**
   ```python
   # calculator.py
   def divide(a, b):
       """두 수를 나눔"""
       return a / b
   ```

2. **테스트 생성 요청**
   ```bash
   > divide 함수에 대한 포괄적인 테스트 생성해줘
   ```

3. **생성된 테스트 확인**
   - 정상 케이스
   - 0으로 나누기 (ZeroDivisionError)
   - 음수 처리
   - 부동소수점 정밀도

4. **테스트 실행**
   ```bash
   > Use the test-runner subagent to run tests
   ```

5. **실패 확인**
   - ❌ `test_divide_by_zero` 실패
   - 이유: ZeroDivisionError 미처리

6. **코드 수정**
   ```python
   def divide(a, b):
       if b == 0:
           raise ValueError("Cannot divide by zero")
       return a / b
   ```

7. **재실행**
   ```bash
   > 테스트 다시 돌려줘
   ```
   - ✅ 모든 테스트 통과

**예상 소요 시간**: 10분

---

### 시나리오 3: 데이터 분석 파이프라인

**목표**: 데이터 수집 → 분석 → 시각화의 전체 파이프라인 구축

**단계:**

1. **데이터 수집 (web_scraper.py 사용)**
   ```bash
   > python examples/web_scraper.py
   ```

2. **데이터 확인**
   ```bash
   > 수집된 데이터 확인
   ```

3. **분석 요청**
   ```bash
   > Use the data-analyzer subagent to analyze the collected data
   ```

4. **결과 확인**
   - 기초 통계 (건수, 평균 길이 등)
   - 키워드 추출
   - 날짜별 분포

5. **시각화 요청**
   ```bash
   > 날짜별 기사 수를 차트로 보여줘
   ```

**예상 소요 시간**: 15분

---

### 시나리오 4: 전체 서브에이전트 체이닝

**목표**: 여러 서브에이전트를 순차적으로 사용하여 완전한 개발 사이클 경험

**워크플로우:**
```
코드 작성
    ↓
code-reviewer (검토)
    ↓
test-generator (테스트 생성)
    ↓
test-runner (테스트 실행)
    ↓
(수정 필요시 반복)
    ↓
commit-helper (커밋 메시지)
    ↓
Git 커밋
```

**단계:**

1. **코드 작성**
   ```bash
   > 간단한 인증 기능 구현해줘
   ```

2. **자동 체이닝 요청**
   ```bash
   > 코드 리뷰하고, 문제없으면 테스트 생성하고, 테스트 돌리고, 커밋까지 해줘
   ```

3. **메인 에이전트 실행 과정**
   - ① code-reviewer 호출
   - ② Critical 이슈 확인 → 없음
   - ③ test-generator 호출
   - ④ test-runner 호출
   - ⑤ 테스트 통과 확인
   - ⑥ commit-helper 호출
   - ⑦ git commit 실행

4. **결과 확인**
   ```bash
   > git log -1
   ```

**예상 소요 시간**: 5분 (자동화)

**실제 사례:**
이 프로젝트에서 실제로 실행한 예시:
```
Input: "3번: 전체 서브에이전트 체이닝 실행"

Main Agent 실행:
1. TodoWrite (7개 작업 계획)
2. Task(test-generator) → 11개 테스트 생성
3. Task(test-runner) → 41개 테스트 100% 통과
4. Edit (DNS 보호 로직 39줄 추가)
5. Edit (lxml 파서 변경 3곳)
6. Edit (DNS 테스트 6개 추가)
7. Task(test-runner) → 47개 테스트 100% 통과
8. Task(commit-helper) → 커밋 메시지 생성
9. Bash(git commit) → 완료

결과: +384 insertions, -4 deletions
커밋: b517070
```

---

## 서브에이전트 체이닝

### 체이닝이란?

여러 서브에이전트를 순차적으로 연결하여 복잡한 워크플로우를 구성하는 것

### 체이닝 패턴

#### 1. 순차 실행 (Sequential)

```
A → B → C
```

**예시**: 코드 작성 → 리뷰 → 테스트 → 커밋

#### 2. 조건부 실행 (Conditional)

```
A → B?
    ├─ Yes → C
    └─ No  → D
```

**예시**: 리뷰 → Critical 있음?
- Yes → 수정 후 재리뷰
- No → 테스트 진행

#### 3. 병렬 실행 (Parallel)

```
    ┌─ B ─┐
A ──┼─ C ─┼→ D
    └─ E ─┘
```

**예시**: 코드 리뷰 + 성능 테스트 동시 실행

### 체이닝 예시

#### 예시 1: 간단한 체이닝

```bash
> 코드 리뷰하고 문제없으면 커밋해줘
```

**실행 순서:**
1. code-reviewer 실행
2. Critical 이슈 확인
3. 없으면 → commit-helper 실행
4. git commit

#### 예시 2: 복잡한 체이닝

```bash
> 보안 리뷰 → 테스트 생성 → 실행 → 커버리지 확인 → 커밋
```

**실행 순서:**
1. code-reviewer (보안 집중)
2. 이슈 수정
3. test-generator
4. test-runner
5. 커버리지 < 80%? → 테스트 추가
6. commit-helper
7. git commit

### 체이닝 베스트 프랙티스

1. **명확한 조건 지정**
   ```bash
   # 좋은 예
   > 코드 리뷰하고 Critical 이슈 없으면 테스트 생성

   # 나쁜 예
   > 코드 리뷰하고 테스트
   ```

2. **중간 결과 확인**
   ```bash
   > 리뷰 → (결과 확인) → 테스트
   ```

3. **실패 시 중단**
   ```bash
   > 테스트 실패하면 커밋 안 함
   ```

---

## 개발 가이드

### 새로운 서브에이전트 추가

#### 1. 파일 생성

```bash
touch .claude/agents/your-agent-name.md
```

#### 2. YAML Frontmatter 작성

```markdown
---
name: your-agent-name
description: 간단한 설명 (자동 활성화 키워드 포함)
tools: Read, Grep, Glob, Bash, Write
model: sonnet
---
```

**필드 설명:**
- `name`: 서브에이전트 고유 ID (소문자, 하이픈)
- `description`: 기능 설명 (키워드 포함)
- `tools`: 사용 가능한 도구 (쉼표로 구분)
- `model`: AI 모델 (sonnet, opus, haiku)

#### 3. 시스템 프롬프트 작성

```markdown
당신은 [역할] 전문가입니다.

## 작업 프로세스

1. [단계 1]
2. [단계 2]
3. [단계 3]

## 체크리스트

- [ ] 항목 1
- [ ] 항목 2

## 출력 형식

[예시 출력]
```

#### 4. 테스트

```bash
> Use the your-agent-name subagent to [작업]
```

#### 5. 확인

```bash
> /agents
```

---

### 새로운 스킬 추가

#### 1. 디렉토리 생성

```bash
mkdir -p .claude/skills/your-skill-name
```

#### 2. SKILL.md 작성

```markdown
---
name: your-skill-name
description: 기능 설명. 키워드: 키워드1, 키워드2
allowed-tools: Bash, Read, Grep
---

# Your Skill Name

이 스킬은 [목적]을 수행합니다.

## 작업 프로세스

1. [단계 1]
2. [단계 2]

## 예시

[예시 코드/결과]
```

#### 3. 지원 스크립트 추가 (선택)

```bash
# .claude/skills/your-skill-name/helper.py
def do_something():
    pass
```

#### 4. 테스트

```bash
> [키워드] 실행
```

---

### 모델 선택 가이드

| 모델 | 특징 | 사용 예시 |
|------|------|----------|
| **Sonnet** | 균형 (속도 + 품질) | 대부분의 서브에이전트 |
| **Opus** | 최고 품질 | 복잡한 분석, 아키텍처 설계 |
| **Haiku** | 최고 속도 | 간단한 작업, 빠른 응답 |

---

## 예제 코드 실행

### 웹 스크레이퍼

#### Python 버전

```bash
python examples/python/web_scraper.py
```

#### TypeScript 버전

```bash
npm run scraper
# 또는
tsx examples/typescript/web-scraper.ts
```

**공통 기능:**
- HTML 파싱 (Python: BeautifulSoup + lxml / TypeScript: Cheerio)
- SSRF 보호 (URL 검증)
- DNS Rebinding 방어
- SSL 검증
- Rate Limiting

**보안 기능:**
- localhost 차단
- 내부 IP 차단 (10.x, 172.16.x, 192.168.x)
- AWS 메타데이터 엔드포인트 차단 (169.254.169.254)
- DNS 해석 결과 검증
- 링크 로컬 IP 차단

### 데이터 처리

#### Python 버전

```bash
python examples/python/data_processor.py
```

#### TypeScript 버전

```bash
npm run processor
# 또는
tsx examples/typescript/data-processor.ts
```

**공통 기능:**
- CSV/JSON 로드
- 데이터 조작 및 변환
- 통계 계산 (평균, 중앙값, 표준편차, 사분위수)
- 데이터 정제 (중복 제거, 결측치 처리)
- 데이터 집계 및 그룹화

### API 클라이언트

#### Python 버전

```bash
python examples/python/api_client.py
```

#### TypeScript 버전

```bash
npm run api
# 또는
tsx examples/typescript/api-client.ts
```

**공통 기능:**
- REST API 통신 (GET, POST, PUT, PATCH, DELETE)
- 재시도 로직 (지수 백오프)
- 에러 처리
- 세션 관리
- JSONPlaceholder API 테스트 예제

---

## 테스트 실행

### 모든 테스트 실행

```bash
pytest
```

### 상세 출력

```bash
pytest -v
```

### 특정 파일 테스트

```bash
pytest tests/test_web_scraper.py
```

### 커버리지 리포트

```bash
pytest --cov=. --cov-report=html
```

리포트 확인:
```bash
open htmlcov/index.html
```

### 테스트 통계

- **총 테스트**: 47개
- **테스트 클래스**: 7개
  - HTML 파싱 오류 (5)
  - URL 검증 & SSRF (6)
  - SSL 검증 (4)
  - 초기화 (11)
  - 정상 기능 (11)
  - 엣지 케이스 (4)
  - DNS Rebinding 보호 (6)
- **커버리지**: 92%

---

## 문제 해결

### 서브에이전트가 활성화되지 않을 때

#### 증상
```bash
> Use the code-reviewer subagent
→ "code-reviewer subagent not found"
```

#### 해결 방법

1. **파일 위치 확인**
   ```bash
   ls .claude/agents/code-reviewer.md
   ```

2. **YAML Frontmatter 검증**
   ```bash
   head -n 10 .claude/agents/code-reviewer.md
   ```

   확인 사항:
   - `---`로 시작하고 끝나는가?
   - `name` 필드가 있는가?
   - `description` 필드가 있는가?

3. **명시적 호출**
   ```bash
   > Use the code-reviewer subagent to review my code
   ```

4. **등록 확인**
   ```bash
   > /agents
   ```

---

### 스킬이 발견되지 않을 때

#### 증상
```bash
> 커밋 메시지 작성
→ 일반 응답 (스킬 미활성화)
```

#### 해결 방법

1. **디렉토리 구조 확인**
   ```bash
   ls .claude/skills/commit-helper/SKILL.md
   ```

2. **SKILL.md 파일명 확인**
   - 반드시 `SKILL.md` (대문자)

3. **키워드 확인**
   ```markdown
   ---
   description: ... 키워드: 커밋, commit, 메시지
   ---
   ```

4. **명시적 활성화**
   - description에 키워드 추가
   - 여러 키워드 사용

---

### 도구 권한 오류

#### 증상
```
Error: Tool 'Write' not allowed for this agent
```

#### 해결 방법

1. **tools 필드 확인**
   ```markdown
   ---
   tools: Read, Grep, Glob, Bash, Write
   ---
   ```

2. **필요한 도구만 추가**
   - 보안상 최소 권한 원칙
   - 불필요한 도구 제거

---

### 성능 최적화

#### 서브에이전트가 느릴 때

1. **모델 변경**
   ```markdown
   ---
   model: haiku  # sonnet → haiku
   ---
   ```

2. **도구 제한**
   ```markdown
   ---
   tools: Read, Grep  # Bash, Glob 제거
   ---
   ```

3. **프롬프트 최적화**
   - 불필요한 지시 제거
   - 명확한 출력 형식 지정

---

## 참고 자료

### 공식 문서

- [Claude Code 공식 사이트](https://code.claude.com)
- [서브에이전트 가이드](https://code.claude.com/docs/en/sub-agents.md)
- [스킬 가이드](https://code.claude.com/docs/en/skills.md)
- [Claude API 문서](https://docs.anthropic.com)

### 표준 및 컨벤션

- [Conventional Commits](https://www.conventionalcommits.org/) - 커밋 메시지 표준
- [PEP 8](https://peps.python.org/pep-0008/) - Python 스타일 가이드
- [pytest 문서](https://docs.pytest.org/) - 테스트 프레임워크

### 보안 참고

- [OWASP Top 10](https://owasp.org/www-project-top-ten/) - 웹 취약점 Top 10
- [SSRF 방어 가이드](https://cheatsheetseries.owasp.org/cheatsheets/Server_Side_Request_Forgery_Prevention_Cheat_Sheet.html)
- [DNS Rebinding 공격](https://en.wikipedia.org/wiki/DNS_rebinding)

### 프로젝트 내부

- [CLAUDE.md](CLAUDE.md) - 프로젝트 전체 가이드
- [examples/](examples/) - Python 예제 코드
- [tests/](tests/) - 테스트 코드

---

## 라이선스

이 프로젝트는 **학습 목적**으로 제공됩니다.

---

## 기여

이슈 및 PR 환영합니다!

1. Fork
2. Feature 브랜치 생성
3. 커밋 (Conventional Commits 사용)
4. Push
5. Pull Request

---

## 문의

프로젝트 관련 문의사항은 Issue로 남겨주세요.

---

**마지막 업데이트**: 2025-12-26
**프로젝트 버전**: 1.0.0
**테스트 통과율**: 100% (47/47)
**커버리지**: 92%
